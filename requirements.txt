Pillow>=10

# Raspberry Pi display only: the board is drawn fullscreen on the Hosyond 7"
# DSI LCD via pygame. Install on the Pi (`pip install pygame`). On a Mac it's
# optional — without it, board.py --once just writes board.png and you preview
# with serve.py.
pygame>=2.1
